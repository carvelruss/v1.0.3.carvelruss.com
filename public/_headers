# Hashed assets — cache forever (Vite content-hashes guarantee uniqueness)
/assets/*
  Cache-Control: public, max-age=31536000, immutable

# All other routes serve index.html (SPA) — never cache so browsers
# always get the latest chunk manifest after each deployment
/*
  Cache-Control: no-cache, no-store, must-revalidate
