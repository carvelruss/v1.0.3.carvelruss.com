name = "portfolio"
compatibility_date = "2024-09-23"
pages_build_output_dir = "dist"

# ── D1 Database ───────────────────────────────────────────────────────────────
# After running `wrangler d1 create portfolio-db`, paste the returned
# database_id below and commit the file.
[[d1_databases]]
binding = "DB"
database_name = "portfolio-db"
database_id = "af268b31-12e9-49f2-a704-ed492a472819"

# ── R2 Bucket (run: wrangler r2 bucket create portfolio-assets) ───────────────
[[r2_buckets]]
binding = "STORAGE"
bucket_name = "portfolio-assets"

# ── Secrets (set via CLI, NOT stored here) ────────────────────────────────────
# wrangler secret put ADMIN_PASSWORD
# wrangler secret put JWT_SECRET
# wrangler secret put TURNSTILE_SECRET